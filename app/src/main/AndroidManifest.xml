<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="arrol.com.xiaomi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.BlinkActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".view.activity.MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".view.activity.AddRecordActivity" />
        <activity android:name=".view.activity.sq_helper.PsRecordActivity" />
        <activity
            android:name=".view.activity.sq_helper.Sq_AddActivity"
            android:theme="@android:style/Theme.DeviceDefault.Dialog.NoActionBar" />
        <activity
            android:name=".view.activity.sq_helper.Sq_update"
            android:theme="@android:style/Theme.DeviceDefault.Dialog.NoActionBar" />
        <activity android:name=".view.activity.alert.AddAlertActivity" />
        <activity android:name=".view.activity.alert.AlarmActivity"
            android:theme="@android:style/Theme.DeviceDefault.Dialog.NoActionBar"
            android:launchMode="singleInstance"
            />
    </application>

</manifest>